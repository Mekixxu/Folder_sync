<UserControl x:Class="FolderSync.UI.Views.TaskEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题 -->
        <Border Grid.Row="0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="20,10,20,0">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignToolButton}" Command="{Binding CancelCommand}" Margin="0,0,10,0">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
                <TextBlock Text="新建/编辑同步任务" Style="{StaticResource PageTitleTextStyle}" />
            </StackPanel>
        </Border>

        <!-- 中间选项卡配置区域 -->
        <TabControl Grid.Row="1" Margin="20,10" Style="{StaticResource MaterialDesignFilledTabControl}" Padding="6">
            <!-- 1. 基础配置 (路径与模式) -->
            <TabItem Header="基础配置" IsSelected="True">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                    <StackPanel>
                        <!-- 任务名称 -->
                        <TextBox materialDesign:HintAssist.Hint="任务名称 (例如: 工作文档备份)" Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                                 Text="{Binding TaskName, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Margin="0,10,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 源路径配置 -->
                            <materialDesign:Card Grid.Column="0" Padding="15" UniformCornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="源目录 (A)" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                                    <ComboBox materialDesign:HintAssist.Hint="协议" ItemsSource="{Binding Protocols}" SelectedItem="{Binding SourceProtocol}" Margin="0,0,0,15"/>
                                    <TextBox materialDesign:HintAssist.Hint="路径 (C:\Data 或 \\192.168.1.10\Share)" Text="{Binding SourcePath}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,10"/>
                                    <Button Content="浏览文件夹" Style="{StaticResource SecondaryActionButtonStyle}" Command="{Binding BrowseSourceCommand}" HorizontalAlignment="Right" Margin="0"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:PackIcon Grid.Column="1" Kind="ArrowRightBold" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                            <!-- 目标路径配置 -->
                            <materialDesign:Card Grid.Column="2" Padding="15" UniformCornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="目标目录 (B)" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                                    <ComboBox materialDesign:HintAssist.Hint="协议" ItemsSource="{Binding Protocols}" SelectedItem="{Binding DestProtocol}" Margin="0,0,0,15"/>
                                    <TextBox materialDesign:HintAssist.Hint="路径 (D:\Backup 或 /ftp/data/)" Text="{Binding DestPath}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,10"/>
                                    <Button Content="浏览文件夹" Style="{StaticResource SecondaryActionButtonStyle}" Command="{Binding BrowseDestCommand}" HorizontalAlignment="Right" Margin="0"/>
                                </StackPanel>
                            </materialDesign:Card>
                        </Grid>

                        <!-- 同步模式与比对策略 -->
                        <TextBlock Text="同步模式与策略" FontWeight="Bold" FontSize="16" Margin="0,30,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="选择同步模式" ItemsSource="{Binding SyncModes}" SelectedItem="{Binding SelectedSyncMode}" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="选择比对引擎" ItemsSource="{Binding DiffStrategies}" SelectedItem="{Binding SelectedDiffStrategy}" Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        </Grid>
                        
                        <TextBlock Text="{Binding ModeDescription}" TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,10,0,0" FontSize="13"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 2. 调度与触发器 -->
            <TabItem Header="计划任务">
                <StackPanel Margin="10,20">
                    <TextBlock Text="触发方式配置" FontWeight="Bold" FontSize="16" Margin="0,0,0,20"/>
                    <RadioButton Content="手动运行 (不自动触发)" IsChecked="{Binding IsManualTrigger}" Margin="0,5" GroupName="TriggerType"/>
                    <RadioButton Content="周期性运行 (如每隔 N 分钟)" IsChecked="{Binding IsPeriodicTrigger}" Margin="0,5" GroupName="TriggerType"/>
                    <RadioButton Content="定时运行 (使用 Cron 表达式)" IsChecked="{Binding IsCronTrigger}" Margin="0,5" GroupName="TriggerType"/>

                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,20,0,0">
                        <StackPanel>
                            <!-- 周期运行配置区 -->
                            <StackPanel Visibility="{Binding IsPeriodicTrigger, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="每隔多长时间执行一次？" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding IntervalValue}" Width="100" materialDesign:HintAssist.Hint="数值" Margin="0,0,15,0"/>
                                    <ComboBox SelectedItem="{Binding IntervalUnit}" Width="100">
                                        <ComboBoxItem Content="秒"/>
                                        <ComboBoxItem Content="分钟"/>
                                        <ComboBoxItem Content="小时"/>
                                        <ComboBoxItem Content="天"/>
                                    </ComboBox>
                                </StackPanel>
                            </StackPanel>

                            <!-- Cron 运行配置区 -->
                            <StackPanel Visibility="{Binding IsCronTrigger, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="输入高级 Cron 表达式 (例如 0 0 12 ? * MON 表示每周一中午12点)：" Margin="0,0,0,10"/>
                                <TextBox Text="{Binding CronExpression}" materialDesign:HintAssist.Hint="0 0 12 ? * *" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                <TextBlock Text="支持秒、分、时、日、月、周配置。" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,5,0,0" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>

            <!-- 3. 过滤规则 -->
            <TabItem Header="过滤规则 (黑/白名单)">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                    <StackPanel>
                        <TextBlock Text="白名单与黑名单可同时配置；白/黑都留空时，默认全部允许。" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 白名单 -->
                            <materialDesign:Card Grid.Column="0" Padding="15" UniformCornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="白名单 (包括)" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                                    <TextBox materialDesign:HintAssist.Hint="扩展名（逗号分隔，例如: .docx, .pdf）" Text="{Binding WhitelistExtensionFilterText}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,10"/>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="最小(MB): " VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Grid.Column="1" Text="{Binding WhitelistMinSizeMB}" materialDesign:HintAssist.Hint="留空不限" Margin="0,0,15,0"/>
                                        <TextBlock Grid.Column="2" Text="最大(MB): " VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Grid.Column="3" Text="{Binding WhitelistMaxSizeMB}" materialDesign:HintAssist.Hint="留空不限"/>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="仅包括新于 " VerticalAlignment="Center"/>
                                        <TextBox Text="{Binding WhitelistNewerThanHours}" Width="80" TextAlignment="Center" Margin="10,0"/>
                                        <TextBlock Text=" 小时的文件" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBox materialDesign:HintAssist.Hint="正则（匹配文件名/路径）" Text="{Binding WhitelistRegexPattern}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <!-- 黑名单 -->
                            <materialDesign:Card Grid.Column="2" Padding="15" UniformCornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="黑名单 (排除)" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>
                                    <TextBox materialDesign:HintAssist.Hint="扩展名（逗号分隔，例如: .tmp, .bak）" Text="{Binding BlacklistExtensionFilterText}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,10"/>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="最小(MB): " VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Grid.Column="1" Text="{Binding BlacklistMinSizeMB}" materialDesign:HintAssist.Hint="留空不限" Margin="0,0,15,0"/>
                                        <TextBlock Grid.Column="2" Text="最大(MB): " VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBox Grid.Column="3" Text="{Binding BlacklistMaxSizeMB}" materialDesign:HintAssist.Hint="留空不限"/>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="排除新于 " VerticalAlignment="Center"/>
                                        <TextBox Text="{Binding BlacklistNewerThanHours}" Width="80" TextAlignment="Center" Margin="10,0"/>
                                        <TextBlock Text=" 小时的文件" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBox materialDesign:HintAssist.Hint="正则（匹配文件名/路径）" Text="{Binding BlacklistRegexPattern}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                                </StackPanel>
                            </materialDesign:Card>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- 底部保存按钮 -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{StaticResource SecondaryActionButtonStyle}" Command="{Binding CancelCommand}" Width="100"/>
                <Button Content="保存任务" Command="{Binding SaveTaskCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Width="150" Margin="0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
