<UserControl x:Class="FolderSync.UI.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="20,10,20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="任务管理" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                
                <Button Grid.Column="2" Content="+ 新建同步任务" Command="{Binding CreateNewTaskCommand}" 
                        Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="18"/>
            </Grid>
        </Border>

        <!-- 任务列表区域 -->
        <ListView Grid.Row="1" Margin="20" ItemsSource="{Binding Tasks}" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Margin="0,5,0,5" Padding="15" UniformCornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 左侧状态图标 -->
                            <materialDesign:PackIcon Grid.Column="0" Kind="{Binding StatusIcon}" 
                                                     Foreground="{Binding StatusColor}" 
                                                     Width="40" Height="40" VerticalAlignment="Center" Margin="0,0,15,0"/>

                            <!-- 中间任务详情 -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="{Binding TaskName}" FontSize="16" FontWeight="Bold"/>
                                    <Border Background="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="5,2" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding SyncMode}" FontSize="11" Foreground="{DynamicResource MaterialDesignBody}"/>
                                    </Border>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <materialDesign:PackIcon Kind="FolderSearchOutline" Width="16" Height="16" Foreground="Gray" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SourcePath}" Foreground="Gray" Margin="5,0,10,0" VerticalAlignment="Center"/>
                                    
                                    <materialDesign:PackIcon Kind="ArrowRight" Width="16" Height="16" Foreground="Gray" VerticalAlignment="Center"/>
                                    
                                    <materialDesign:PackIcon Kind="FolderUploadOutline" Width="16" Height="16" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    <TextBlock Text="{Binding DestPath}" Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                
                                <TextBlock Text="{Binding ScheduleInfo}" Foreground="DarkGray" FontSize="12" Margin="0,5,0,0"/>
                            </StackPanel>

                            <!-- 右侧操作按钮 -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}" 
                                        Command="{Binding DataContext.RunTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="立即运行" Margin="5,0">
                                    <materialDesign:PackIcon Kind="Play" Width="20" Height="20"/>
                                </Button>
                                
                                <Button Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}" 
                                        Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="编辑任务" Margin="5,0">
                                    <materialDesign:PackIcon Kind="Pencil" Width="20" Height="20"/>
                                </Button>
                                
                                <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Background="#F44336" BorderBrush="#F44336"
                                        Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                        CommandParameter="{Binding}"
                                        ToolTip="删除任务" Margin="5,0">
                                    <materialDesign:PackIcon Kind="TrashCan" Width="20" Height="20" Foreground="White"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
            
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListView">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="TextBoxSearchOutline" Width="80" Height="80" Foreground="{DynamicResource MaterialDesignDivider}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="暂无同步任务" FontSize="18" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                                            <TextBlock Text="点击右上角 [新建同步任务] 开始" FontSize="14" Foreground="DarkGray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
        </ListView>
    </Grid>
</UserControl>
