<UserControl x:Class="FolderSync.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部统计卡片 -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,18">
            <!-- 卡片1: 活动任务 -->
            <materialDesign:Card Margin="10,6" Padding="18" Background="{StaticResource AppSurfaceBrush}" UniformCornerRadius="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="FormatListChecks" Width="22" Height="22" VerticalAlignment="Center" Foreground="{StaticResource InfoBrush}"/>
                        <TextBlock Text="{DynamicResource Dashboard.ActiveTasks}" FontSize="16" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ActiveTaskCount}" FontSize="34" FontWeight="Bold" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- 卡片2: 今日同步文件数 -->
            <materialDesign:Card Margin="10,6" Padding="18" Background="{StaticResource AppSurfaceBrush}" UniformCornerRadius="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="FileDocumentMultiple" Width="22" Height="22" VerticalAlignment="Center" Foreground="{StaticResource SuccessBrush}"/>
                        <TextBlock Text="{DynamicResource Dashboard.TodaySynced}" FontSize="16" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TodaySyncCount}" FontSize="34" FontWeight="Bold" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- 卡片3: 失败任务数 -->
            <materialDesign:Card Margin="10,6" Padding="18" Background="{StaticResource AppSurfaceBrush}" UniformCornerRadius="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="AlertCircleOutline" Width="22" Height="22" VerticalAlignment="Center" Foreground="{StaticResource DangerBrush}"/>
                        <TextBlock Text="{DynamicResource Dashboard.TodayErrors}" FontSize="16" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TodayErrorCount}" FontSize="34" FontWeight="Bold" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>

        <!-- 下方任务列表概览 -->
        <materialDesign:Card Grid.Row="1" Margin="10,6" UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="18">
                    <TextBlock Text="{DynamicResource Dashboard.RunningTasks}" Style="{StaticResource SectionTitleTextStyle}"/>
                </Border>

                <!-- 这里后续可以绑定一个 ListView -->
                <ListView Grid.Row="1" Margin="10" ItemsSource="{Binding ActiveTasks}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="任务名称" DisplayMemberBinding="{Binding TaskName}" Width="150"/>
                            <GridViewColumn Header="源路径" DisplayMemberBinding="{Binding SourcePath}" Width="200"/>
                            <GridViewColumn Header="目标路径" DisplayMemberBinding="{Binding DestinationPath}" Width="200"/>
                            <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="100"/>
                            <GridViewColumn Header="进度" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <ProgressBar Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100" Height="10" Margin="0,5"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    <!-- 占位提示 -->
                    <ListView.Style>
                        <Style TargetType="ListView">
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListView">
                                                <TextBlock Text="{DynamicResource Dashboard.EmptyRunning}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontSize="16"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.Style>
                </ListView>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
