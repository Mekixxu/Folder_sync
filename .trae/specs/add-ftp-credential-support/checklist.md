- [x] FTP 协议下可选择 `匿名登录` 或 `账号密码登录`

- [x] 选择 `账号密码登录` 时，用户名和密码为必填，缺失时不能保存任务

- [x] FTP 路径禁止内嵌 `user:password@` 形式的凭据，并在保存时给出明确提示

- [x] 源端和目标端可分别配置各自的 FTP 认证信息

- [x] 密码不会以明文形式写入 `data/tasks.json`

- [x] 已保存的 FTP 密码使用 Windows DPAPI 加密，并可在任务运行时成功解密使用

- [x] Quartz 调度触发的 FTP 任务可自动恢复已保存凭据并正常连接

- [x] 旧版仅包含 FTP 路径的任务会自动按 `匿名登录` 兼容运行

- [x] FTP 连接创建逻辑不再固定使用 `anonymous / anonymous@`

- [x] 关键回归场景通过：匿名登录、账号密码登录、路径内嵌凭据拦截、定时任务运行、历史任务兼容
