* [x] 过滤规则一级已改为白名单（包括）与黑名单（排除），并支持同时配置

* [x] 白名单与黑名单均可配置扩展名、文件大小、文件时间（hour）、正则（文件名/路径）

* [x] “全部允许”选项已移除，且白/黑名单均为空时行为为全部允许

* [x] 时间过滤粒度已从 day 改为 hour，并在 UI 与评估逻辑一致

* [x] 保存时若检测到白/黑名单冲突，会提示并阻止保存

* [x] 关键回归场景验证通过（空规则、冲突规则、小时过滤）

