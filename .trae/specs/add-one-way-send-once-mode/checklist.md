- [x] 新增独立同步模式 `单向一次性同步`，且不改变现有 `单向增量 / 单向更新 / 单向镜像 / 双向同步` 的语义

- [x] 一次性同步任务在首次成功复制文件后，会为该任务记录成功投递状态

- [x] 当 B 端后续删除已投递文件时，任务再次运行不会补发该文件

- [x] 复制失败时不会写入成功投递状态，后续任务仍可重试

- [x] 状态存储至少以 `task_id + relative_path` 唯一标识已投递文件，并保留源端快照字段

- [x] 分析窗口与执行前结果能清晰显示“已同步过，按一次性规则跳过”

- [x] 同一路径内容变化时默认跳过，并在日志或分析中给出 warning

- [x] 报告中包含“已同步过跳过”的统计项

- [x] 用户可对单个任务执行“一次性同步状态重置”，重置后允许重新投递

- [x] 关键回归场景通过：首次同步、B 端删除不补发、失败重试、状态重置后重发
